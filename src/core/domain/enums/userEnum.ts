export enum GenderType {
    Male = "male",
    Female = "female",
  }

  export enum RoleType {
    Client = "client",
    Admin = "admin",
  }

  export enum StatusType {
    Active = "actived",
    InActive = "Inactive",
    Archived = "Archived",
  }

  export enum TypeUse {
    Normal = "normal",
    WithGG = "withgg"
  }

  export enum Pay {
    IsPay = "ispay",
    NotPay = "notpay"
  }